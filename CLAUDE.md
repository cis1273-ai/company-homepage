# CLAUDE.md

## 보안 규칙 (필수)

- **API 키·비밀번호는 코드에 직접 쓰지 않는다.** `.env` 파일에 넣고 환경변수로 불러 쓴다.
- **`.env` 는 반드시 `.gitignore` 에 넣는다.** 커밋 대상은 값이 빈 `.env.example` 뿐이다.
- **외부 서비스 호출은 화면(브라우저) 쪽이 아니라 서버 쪽에서 한다.**
  - 이 프로젝트는 Vite + React 정적 SPA 라 `VITE_*` 값이 클라이언트 번들에 그대로 포함된다.
    브라우저에 노출되면 안 되는 값(관리자 비밀번호, API 키)은 Google Apps Script 등
    백엔드로 옮기고, 거기서 인증·검증한 뒤 호출해야 한다.
- **데이터를 보여주기 전에 요청한 사람이 그 데이터의 주인(또는 권한자)인지 반드시 확인한다.**
  - 백엔드(Apps Script) 엔드포인트는 호출자 신원/토큰을 검증한 뒤에만 문의 데이터를 반환한다.
  - 상세 화면뿐 아니라 목록 조회에도 동일한 권한 검사를 적용한다.
- **라이브러리는 최신 버전을 쓴다.** 구버전을 유지해야 하면 그 이유를 주석이나 문서에 남긴다.

## 프로젝트 개요

- Vite + React 19 + Tailwind v4 로 만든 회사 소개 SPA.
- 상담 문의 접수/관리는 Google Apps Script(`SCRIPT_URL`) 백엔드 + Google Sheet 를 사용.
- `#admin` 해시로 관리자 화면 진입. 현재 인증이 클라이언트에서만 이뤄짐 → `docs/SECURITY_AUDIT.md` 참고.
